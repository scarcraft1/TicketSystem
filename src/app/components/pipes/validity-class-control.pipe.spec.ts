import { ValidityClassControlPipe } from './validity-class-control.pipe';

describe('ValidityClassControlPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidityClassControlPipe();
    expect(pipe).toBeTruthy();
  });
});
